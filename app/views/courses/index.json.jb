@courses.map do |course|
  {
    id: course.id,
    name: course.name,
    grade: course.grade,
    credits: course.credits,
    level: course.level,
    semester_taken: course.semester_taken,
    point_value: course.point_value,
  }
end
